{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next export",
    "deploy": "npm run build && npm run export && touch out/.nojekyll && gh-pages -d out"
  },
  "dependencies": {
    // your existing dependencies...
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
